import BookingPage from "@/app/components/ui/booking-page";

export default function StartProjectPage() {
  return (
    <BookingPage
      eyebrow="Start a Project"
      heading="Schedule a Call"
      subheading="Let's bring your idea to life. Tell us about your project and pick a time — we'll take it from there."
      successHeading="Call scheduled!"
      successBody="We'll send a confirmation to"
    />
  );
}
